import { Injectable } from '@angular/core';

@Injectable()
export class SessionService {

  // create session
  // login, logout, store the user object and expose info about it
  // store authenticated config object
}
